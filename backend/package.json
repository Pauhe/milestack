{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Milestack backend and indexer scaffold",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc --noEmit",
    "test": "tsx --test --test-concurrency=1",
    "coverage": "c8 --all --src src --exclude '**/*.test.ts' --exclude '**/*.test.tsx' --reporter=text-summary --reporter=lcov --reporter=json-summary --report-dir coverage tsx --test --test-concurrency=1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "express": "^5.2.1",
    "viem": "^2.48.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^5.0.6",
    "@types/node": "^25.6.0",
    "c8": "^10.1.3",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
